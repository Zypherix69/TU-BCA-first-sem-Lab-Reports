#include<stdio.h>
   int main(){
       float A, P, R;
       printf("Enter the Radius of te circle : ");
       scanf("%f",&R);
       printf("Area of the Circle is %.2f" , 3.1415 * R * R);
       printf("Circumference of the Circle is %.2f", 2*3.1415 * R * R);
       return 0;
       }